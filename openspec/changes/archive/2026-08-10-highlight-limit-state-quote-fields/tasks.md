## 1. 共用漲跌停狀態

- [x] 1.1 先為共用漲跌停 primitive 加入單元測試，涵蓋合法漲停 `271.50`／`+9.92%`、合法跌停、上下限價獨立驗證、超界值、一般價、無效數值與指數 fail-closed。
- [x] 1.2 實作共用漲跌停 primitive，僅比較目前顯示最新價與有效 `limit_up`／`limit_down`，並讓 QuoteBoard 移除元件內巢狀判定、改用共用結果。

## 2. 報價群組反白

- [x] 2.1 調整 QuoteBoard 的最新價、漲跌與漲跌幅為單一可反白報價群組，加入可測試狀態，套用 theme 實心背景與高對比文字及現有響應式排列；依後續回饋移除可見的「漲停／跌停」徽章，改保留可存取文字。
- [x] 2.2 讓自選清單使用共用漲跌停判定，只反白每列右側最新價、漲跌與漲跌幅群組，補上非純色彩的可存取狀態，並保持選取、hover、成交 flash、拖放與走勢縮圖行為。
- [x] 2.3 讓托盤迷你自選使用共用漲跌停判定，只反白最新價與漲跌幅群組並補上可存取狀態，不改變商品代碼、名稱、走勢縮圖或點選行為。
- [x] 2.4 確認一般價、漲停打開及跌停打開時即時移除持續反白，且行情摘要、五檔、逐筆成交、歷史 K 棒、下單價格與損益欄位未被套用本功能樣式。

## 3. 自動化與可見驗收

- [x] 3.1 新增或擴充元件／DOM 測試，驗證三種報價群組的 `up`、`down`、非漲跌停、snapshot fallback、可存取標籤及解鎖狀態，並檢查選取或成交 flash 不會永久覆蓋持續背景。
- [x] 3.2 執行相關單元測試、完整測試、TypeScript／lint 與 production build，修正所有由本 change 造成的失敗。
- [x] 3.3 在 simulation 本機畫面以漲停與跌停案例驗收主 QuoteBoard、自選清單及托盤迷你自選，檢查預設台股紅漲綠跌、國際配色，以及標準畫面與 `1.15`／`1.3` 字體倍率無折行、裁切、重疊或版面位移，保存可辨識的畫面證據。
- [x] 3.4 執行 OpenSpec strict validation 與 `git diff --check`，確認 change artifacts、程式碼與驗收結果一致後再進入歸檔流程。

## 4. 全面報價面板與選取資料流補強

- [x] 4.1 擴充 Contract V2 股票 metadata，保留 `reference`、`limit_up`、`limit_down`，盤點排行榜、類股熱力圖、托盤排行、個股期、權證、選擇權與持倉現價等目前報價來源，所有無可靠上下限價者維持 fail-closed。
- [x] 4.2 建立 snapshot-aware 的共用選取 callback，讓排行榜、熱力圖與衍生品報價列傳遞當時 snapshot；來源沒有 snapshot 時補抓單一商品，並以 selection generation 防止舊合約或 snapshot 回寫。
- [x] 4.3 修正 K 線商品切換，在新 contract code 開始載入時清除前一商品 series／readout，並以 generation／code 防止舊 K 線結果回寫。
- [x] 4.4 讓排行榜及托盤排行依精確 Contract V2 上下限價反白最新價／漲跌資訊，保留分類與成交量的一般樣式及可存取狀態。
- [x] 4.5 讓類股熱力圖、個股期、權證、選擇權及持倉現價的目前報價依相同精確規則反白；五檔、逐筆、歷史 K 棒、下單、損益與市場訊號事件不得被套用。
- [x] 4.6 移除 QuoteBoard 可見的「漲停／跌停」徽章，只保留紅／綠底白字及可存取狀態，並更新既有元件／DOM 測試。
- [x] 4.7 新增選取資料流、快速切換與各報價面板測試，執行完整單元／瀏覽器測試、production build、simulation 可見驗收、OpenSpec strict validation 與 `git diff --check`。
