## 1. 河流圖七線資料與呈現

- [x] 1.1 將 `worker/taiwan-stock-pe-river.ts` 的固定百分位、回應 multiplier、每日價格點與 readout 區間由 P10/P30/P50/P70/P90 擴充為 P5/P20/P35/P50/P65/P80/P95，保留五年、252 筆、verified/provisional 與缺口規則。
- [x] 1.2 更新 `/api/taiwan-stock-pe-river` 的型別、驗證與 fixture，確保七個倍率依線性插值遞增，provisional row 不進入樣本。
- [x] 1.3 更新 `public/static/pe-river-overlay.js` 的鍵值、七條線、六個 band、歷史百分位色彩、ARIA/readout 與 rAF 重繪，使 overlay 保持在 K 線後方且 `pointer-events: none`。
- [x] 1.4 調整河流圖單元與渲染測試，覆蓋七線六帶、缺口、provisional、縮放、PNG 與不顯示投資建議。

## 2. 主圖註記基礎與費波那契

- [x] 2.1 在 `public/static/index.html`、CSS 與 `public/static/app.js` 建立範圍限縮的主圖繪圖工具入口、註記 SVG overlay 與不攔截既有 K 線手勢的事件分層。
- [x] 2.2 建立以 canonical symbol 與 interval 隔離的版本化本機註記儲存、讀取、驗證、清除與 panel 銷毀流程；損毀或過期資料必須 fail-safe。
- [x] 2.3 實作費波那契回撤兩點狀態機與 0/23.6/38.2/50/61.8/78.6/100 水準，依 `B - r × (B - A)` 計算每條價格。
- [x] 2.4 實作費波那契拓展三點狀態機與 61.8/100/127.2/161.8/200/261.8 水準，依 `C + r × (B - A)` 計算每條價格。
- [x] 2.5 將每條費波那契水準呈現為「百分比　格式化價格」，處理商品 tick size、右側標籤安全區、密集標籤避讓與縮放／resize／資料更新後的重新計算。
- [x] 2.6 加入 Escape 取消、明確清除操作、商品／週期切換隔離與完整 panel PNG 擷取，且不影響 Volume Profile、河流圖與副圖。
- [x] 2.7 在三點選取過程與完成結果顯示高對比 A／B／C 圓形錨點及波段導引線；將回撤與拓展改成不同的局部雙層 halo 線型與至少 2px 前景線，避免和既有 K 線、均線、網格、河流或價格範圍混淆。
- [x] 2.8 依教學影片的設點方式補上滑鼠跟隨的下一點預覽：A 後預覽 A→B，A／B 後預覽 B→C 與拓展水準；將回撤水平線限制在 A–B、拓展水平線限制在 B–C，使最後一個水平位置控制向右延伸長度，且暫態狀態不得保存。
- [x] 2.9 依參考圖將回撤與拓展改為約 1.5 至 2 CSS px 彩色實線、相鄰半透明色帶及水準起點左側的「比率（價格）」標籤，移除水平線粗 halo／虛線並保留 A／B／C 圓形錨點與虛線波段導引。
- [x] 2.10 將拓展固定水準調整為 0.618、0.786、1、1.272、1.414、1.618、2，並以不顯示內容的價格軸輔助 series 讓完成與預覽狀態的全部水準可見，清除或切換身份時同步清空。
- [x] 2.11 將費波那契錨點改為小型、約 1.25 CSS px、`fill: none` 且不含 A／B／C 字元的透明空心圓，圓心維持實際選點座標；回撤與拓展水準及色帶由相關波段較左錨點延伸到右側價格軸前的 plot 安全邊界。

## 3. K 線價格範圍

- [x] 3.1 擴充主圖 `contextmenu` 與鍵盤 ContextMenu／Shift+F10 事件，安全解析指向的圖上時間與精確價格，同時保留既有右鍵動作。
- [x] 3.2 在右鍵選單實作「價格距離」：以有效右鍵點作第一錨點，以下一次有效左鍵作第二錨點，並對零、非有限或無法換算價格顯示安全失敗狀態。
- [x] 3.3 繪製價格距離連線或括號及結果文字，正確顯示 `(終點 - 起點)` 與 `((終點 - 起點) / 起點) × 100` 的正負號和格式化值。
- [x] 3.4 將完成量測納入註記本機保存、Escape／清除、商品／週期切換、resize／縮放重繪與完整 panel PNG；暫態量測不得保存。
- [x] 3.5 將「價格距離」改名為「價格範圍」，由主圖繪圖工具啟動後以兩次左鍵設定起終點；第一點後須以滑鼠位置即時預覽，離開或 Escape 時清除暫態內容。
- [x] 3.6 依使用者錄影改繪半透明漲跌矩形、上下水平界線、中央垂直雙向箭頭與上緣中央標籤；標籤顯示價差、漲跌幅及可取得時的最小跳動格數，並遷移舊版 `distance` 本機資料。
- [x] 3.7 將價格範圍改為台股紅漲綠跌，並將上下水平界線縮為 1 CSS px，保留中央雙向箭頭與錨點的視覺層級。

## 4. 主圖交易品種清單字級

- [x] 4.1 為主圖「交易品種」選單加入專屬 CSS：單圖／雙圖 13px、三圖以上 12px，並保留控制項高度、單行截斷、可存取名稱及鍵盤操作。
- [x] 4.2 驗證樣式不影響報價、商品標題、價格軸、副圖或其他 `select` 控制項，且 3/4/6/8 圖版型沒有溢出或水平捲動。
- [x] 4.3 在每個主圖選單加入「數值顯示」選擇器，提供「圖區左上角」與「浮動視窗」，預設左上角並以瀏覽器本機偏好同步所有 panel。
- [x] 4.4 左上角模式未指向 K 線時顯示最新一筆，游標移動時在固定位置更新；浮動模式保留跟隨游標與離開後隱藏，且不得改變副圖 inline readout。

## 5. 測試與正式驗收

- [x] 5.1 新增或更新 Worker、河流 overlay、註記計算與本機儲存測試，覆蓋兩方向回撤、三點拓展、每條線的百分比與價格、價格距離漲跌／無效點及身份隔離。
- [x] 5.2 執行 rendered HTML 與瀏覽器互動驗收：主圖選單、費波那契選點／取消／清除、右鍵價格距離、鍵盤選單、縮放重繪、多圖字級與不干擾既有指標。
- [x] 5.3 驗證完整 panel PNG 同時保留河流七線六帶、費波那契標籤、價格距離、K 線與可見副圖，且沒有裁切或殘留前一商品註記。
- [x] 5.4 執行 lint、完整測試、`openspec validate enhance-main-chart-valuation-and-drawing-tools --strict` 與 `git diff --check`，記錄結果後再依正式 Sites 發布及已登入瀏覽器驗收流程處理。
- [x] 5.5 補齊 A／B／C 選點進度、完成錨點、回撤／拓展差異線型、pointer-events 與 PNG 保留樣式測試，並在實際主圖與密集版型完成視覺辨識驗收。
- [x] 5.6 補齊滑鼠預覽、離開／Escape 清除、A–B／B–C 水平範圍與未寫入本機儲存測試，並以實際主圖驗收兩點回撤、三點拓展及向右延伸行為。
- [x] 5.7 補齊價格範圍兩點狀態、滑鼠預覽、不保存暫態、舊資料遷移、漲跌樣式與 PNG 保留測試，並以實際主圖驗收操作及可見結果。
- [x] 5.8 補齊價格範圍紅漲綠跌與水平界線粗細測試，並於實際主圖驗收起點價位可辨識。
- [x] 5.9 補齊主圖 readout 預設模式、偏好同步、固定／浮動 class、最新值復原與副圖不受影響測試，並於單圖及多圖實際驗收兩種模式。
- [x] 5.10 補齊費波那契小數比率、七條拓展、色帶／細實線／左側標籤、價格軸輔助範圍與 PNG 保留測試，並於本機及已登入正式站實際驗收回撤與拓展全部水準。
- [x] 5.11 補齊透明空心錨點、無字元、精確圓心、水平線／色帶右側安全邊界及 PNG 保留測試，並於本機及已登入正式站確認 K 棒選點價位未被遮擋。
