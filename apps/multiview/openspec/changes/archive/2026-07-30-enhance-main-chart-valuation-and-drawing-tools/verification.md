## 驗證結果

日期：2026-07-25（Asia/Taipei）

### 費波那契參考圖式顯示

- 回撤維持 0、0.236、0.382、0.5、0.618、0.786、1 七個水準；拓展調整為 0.618、0.786、1、1.272、1.414、1.618、2 七個水準。
- 水平水準改為 1.6 CSS px 彩色實線，相鄰水準間有六個低透明度色帶；不再使用水平粗 halo 或水平虛線。
- 標籤使用 `0.236 (價格)` 小數比率格式，優先在水平線起點左側右對齊；波段導引維持細虛線，A／B／C 高對比圓形標籤保留。
- 以不顯示內容的最低／最高價格輔助 series 將拓展水準納入右側價格軸；完成、預覽、取消、清除及身份切換均同步更新。

### 自動化與本機瀏覽器

- `npm run lint` 通過。
- `npm test` 完成建置且 246 項測試全數通過。
- `openspec validate enhance-main-chart-valuation-and-drawing-tools --strict` 與 `git diff --check` 通過。
- 本機實際繪製回撤與拓展：回撤 7 線／6 帶、拓展 7 線／6 帶，標籤均位於起點左側；所有水平線 computed style 為 1.6px、`stroke-dasharray: none`。
- 測試拓展最高 2 水準位於 plot 內，證明價格軸輔助範圍已解決高水準在畫面外只看到 0.618 的問題；本機測試註記完成後已清除。

### 正式 Sites 驗收

- owner-only Sites version 131 已部署成功，對應 runtime commit `f54e3feb78ccaae5180dc9482b2905ce10c4b1e5`。
- 已登入 Chrome 於正式站 `2454.TW` 日 K 實際完成三點拓展；可見 0.618、0.786、1、1.272、1.414、1.618、2 全部七條水準、六個色帶、左側比率／價格標籤及 A／B／C 圓形錨點。
- 正式站測得七條水平線皆為 1.6px 實線，最高 2 水準座標仍在 424px 高的 plot 內。
- 測試用 `2454.TW` 註記已清除，瀏覽器已回到原本 `3231.TW` 頁面；原有 7 條回撤與標籤仍完整存在，未覆蓋使用者既有註記。

## 透明錨點與右側延伸終驗

日期：2026-07-25（Asia/Taipei）

### 實作與自動化

- 費波那契回撤／拓展錨點改為半徑 5.5、`fill: none`、1.25 CSS px 淺色外框；圓心直接使用實際時間／價格座標，不再因邊界裁切而平移。
- 移除錨點內的 A／B／C 可見字元；A、B、C 僅保留在 DOM class 與 `aria-label` 作為選點順序語意，不遮住所選 K 棒與價位。
- 回撤水準由第一、第二錨點的較左座標開始，拓展水準由第二、第三錨點的較左座標開始；所有水平線與相鄰色帶均延伸到右側價格軸前的 plot 安全邊界。
- `tests/chart-annotations.test.mjs` 已覆蓋透明空心錨點、無可見字元、精確圓心、右側安全邊界及既有 PNG 匯出註記保留路徑。
- `npm run lint` 通過；`npm test` 完成建置且 246 項測試全數通過；OpenSpec strict 與 `git diff --check` 通過。

### 本機與正式 Sites 可見驗收

- 本機實際繪製回撤與拓展：圓形錨點 computed style 均為 `fill: none`、`stroke-width: 1.25px`，半徑 5.5，群組內沒有可見文字；回撤與拓展的 7 條水平線及 6 個色帶均到達右側安全邊界。
- owner-only Sites version 133 部署成功，對應 runtime commit `6e44f20fc5a04cb9ef306e87f56a13f107c8b8fc`。
- 已登入 Chrome 於正式站 `2454.TW` 日 K 分別完成三點拓展與兩點回撤：三個／兩個錨點均為透明空心、無 A／B／C 字元，圓內仍可見 K 棒與精確選點位置。
- 正式站拓展與回撤各 7 條水平線、6 個色帶的右端均為 1401px；主圖 surface 寬 1479px，確認線段延伸到價格縱軸前且未進入價格軸。
- 測試用 `2454.TW` 註記已清除，Chrome 分頁已恢復原網址；既有 `2301.TW` 圖形未修改。
