## 1. 預設選擇

- [x] 1.1 將多層副圖首次預設清單排除 `tdcc-holder-count`，保留既有保存選擇與群組全選行為。
- [x] 1.2 更新前端選擇狀態測試，驗證新狀態未勾集保戶數、既有客製狀態不被覆寫。

## 2. 資料集別最新日期

- [x] 2.1 （已由 4.1 取代）原固定時間有效終點方案完成後，依使用者澄清改為來源日期驅動。
- [x] 2.2 （已由 4.2 取代）原資料集有效終點串接完成後，移除固定時間 gate。
- [x] 2.3 （已由 4.3 取代）保留逐資料集驗證與裁切，但改以 provenance 的實際來源日期判定。

## 3. 回歸測試與驗證

- [x] 3.1 （已由 4.4 取代）原固定時間 fallback 測試已重寫為來源日期驅動測試。
- [x] 3.2 （已由 4.4 取代）原 22:00 前裁切測試已重寫為可信來源立即顯示與不可信快取排除測試。
- [x] 3.3 執行 `npm test`、`npm run lint`、`npm run build`、`openspec validate --all --strict` 與 `git diff --check`。
- [x] 3.4 本機驗證集保戶數首次未勾選、三大法人顯示今日資料及信用交易日期仍正確。

## 4. 來源日期驅動修正

- [x] 4.1 依使用者澄清，將規格由固定發布截止改為取得具可驗證來源日期後立即顯示。
- [x] 4.2 移除日籌碼固定時間有效終點，讓 provider 查詢 requested end 並以實際 rows 判定 partial／available。
- [x] 4.3 將 TWSE 融資券 fallback 改為含日期報表，排除無日期快照與舊版未驗證 cache。
- [x] 4.4 新增「8/5 已取得立即顯示」及「只有 8/4 絕不建立 8/5 重複資料」回歸測試。
- [x] 4.5 執行完整測試、lint、build、OpenSpec strict validation、diff check 與本機 API 驗證。
