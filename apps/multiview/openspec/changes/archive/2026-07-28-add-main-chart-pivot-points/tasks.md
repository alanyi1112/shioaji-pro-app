## 1. Pivot Point 計算核心與參考週期

- [x] 1.1 先新增 Traditional Pivot Point 公式測試，涵蓋正常 OHLC、零值、缺值與非有限數值。
- [x] 1.2 實作純函式計算 `P`、`R1`、`R2`、`R3`、`S1`、`S2`、`S3`，並以明確型別回傳七條數列所需資料。
- [x] 1.3 新增所選週期對參考週期的測試：`1m` 至 `4h` 與 `1d` 使用前一交易日、`1wk` 使用前一完整交易週、`1mo` 使用前一完整交易月。
- [x] 1.4 實作依供應商市場時區與實際交易資料尋找前一完整期間的 helper，避免以固定日曆減法或前一根盤中 K 棒代替。
- [x] 1.5 新增跨週末、休市日、跨月與資料開頭不足時的參考期間測試。

## 2. Worker 延遲載入與資料契約

- [x] 2.1 在 `/api/candles` 與 `/api/stream` 解析並正規化 `pivot=traditional`；未指定時維持既有行為。
- [x] 2.2 針對盤中週期，以既有供應商與歷史快取取得日線參考資料，並沿用 single-flight／快取機制避免重複請求。
- [x] 2.3 將 Pivot Point 計算結果對齊所選 K 棒，輸出 `indicators.pivot_points` 的模式、參考週期、狀態與七條等長陣列。
- [x] 2.4 更新 candles、stream 與相關資料快取鍵／契約版本，使啟用與未啟用 Pivot Point 的回應不會互相污染。
- [x] 2.5 新增 `/api/candles` 測試，確認未勾選時不額外取高週期資料，勾選時回傳正確欄位，參考資料不足時不影響其他圖表資料。
- [x] 2.6 新增 `/api/stream` 測試，確認 snapshot 與更新事件沿用相同模式，且同一參考期間內的 Pivot Point 數值保持穩定。
- [x] 2.7 新增向前補歷史資料測試，確認 prepend 後既有與新增 K 棒仍依各自前一完整期間重新對齊。

## 3. 主圖功能表與請求狀態

- [x] 3.1 在主圖功能表加入預設未勾選的「Pivot Point」選項，並更新靜態 HTML／無 JavaScript fallback 契約。
- [x] 3.2 在每個圖表面板保存獨立的 Pivot Point 選取狀態，並讓 candles 與 stream 請求在啟用時附帶 `pivot=traditional`、停用時移除。
- [x] 3.3 實作勾選切換、商品切換與週期切換時的重新載入及 latest-wins 防護，避免舊回應覆蓋新狀態。
- [x] 3.4 確認 Pivot Point 不影響其他主圖／副圖選項，且單圖與八圖模式的功能表都能完整顯示並以鍵盤操作。

## 4. 圖表繪製、讀值與匯出

- [x] 4.1 定義 `P`、`R1` 至 `R3`、`S1` 至 `S3` 的一致樣式，建立七條階梯線 series，並沿用較線寬只大一點的十字線標記尺寸。
- [x] 4.2 實作七條線的建立、資料更新、價格標籤與清除流程，確保相鄰參考期間之間不以斜線連接。
- [x] 4.3 在 Pivot Point 啟用時加入條件式主圖讀值列，依十字線所在 K 棒顯示七個值，離開歷史位置後恢復最新值。
- [x] 4.4 在取消勾選、面板銷毀、圖表重建與商品／週期切換時完整移除 series、標籤、讀值與事件訂閱。
- [x] 4.5 更新完整 PNG 匯出與離屏圖表複製流程，使啟用時包含 Pivot Point、停用時不殘留相關圖層。

## 5. 驗證與交付

- [x] 5.1 以固定 OHLC fixture 精確核對七個公式值，並核對日線、週線、月線使用實際前一完整交易期間。
- [x] 5.2 執行並通過 Pivot Point 計算、API、快取、stream、歷史補載與錯誤隔離測試。
- [x] 5.3 執行並通過主圖功能表、面板狀態、圖表繪製、讀值、清理與 PNG 匯出測試，以及專案既有 lint／build／完整測試。
- [x] 5.4 執行 `openspec validate add-main-chart-pivot-points --strict` 與 `git diff --check`，修正所有規格或格式問題。
- [x] 5.5 在本機瀏覽器以單圖與八圖模式驗收：預設關閉、功能表可見、鍵盤操作、切換顯示、週期映射、歷史讀值、取消後清除、PNG 匯出與 console 無錯誤。
- [x] 5.6 使用者授權發布後，依 Sites 流程部署完整 HEAD，並在正式站重驗單圖／八圖的可見結果、互動、資產版本與 console。
