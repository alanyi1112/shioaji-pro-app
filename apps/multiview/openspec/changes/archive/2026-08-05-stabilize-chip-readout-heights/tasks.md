## 1. 建立穩定高度的測試基線

- [x] 1.1 為讀值版面簽章與最大內容包絡建立純函式測試，涵蓋完整資料、部分資料、無資料、最近一筆及無前一筆等狀態。
- [x] 1.2 建立瀏覽器幾何驗收工具，記錄標題列高度、副圖高度、下一層副圖頂端、面板高度及十字線 X 座標。
- [x] 1.3 加入 source contract 測試，禁止 pointer／crosshair 更新路徑量測 DOM、調整圖表尺寸或呼叫跨面板高度協調器。

## 2. 實作單一副圖的高度預留

- [x] 2.1 將各籌碼副圖的讀值內容整理為可重用的資料模型與最大內容候選，維持既有數值、顏色、單位及資料狀態文字。
- [x] 2.2 實作版面簽章，納入模式、副圖寬度、字型／縮放、已選序列、資料狀態族群及大戶／散戶門檻。
- [x] 2.3 實作有界的隱藏量測器，每個副圖與版面簽章只進行一次必要量測，且不進入無障礙樹與匯出內容。
- [x] 2.4 以 CSS 變數或等效機制套用讀值區預留高度，保留完整換行文字，不使用省略、裁切、橫向捲動或 tooltip 取代。
- [x] 2.5 調整即時讀值與最新值還原流程，使 crosshair 移動期間只更新內容與樣式，不改變任何副圖幾何。
- [x] 2.6 在副圖卸載、模式切換及 controller 銷毀時清除量測器、簽章快取與高度樣式。

## 3. 實作相容面板的同列高度協調

- [x] 3.1 擴充副圖 controller，提供回報本地預留高度、套用同列預留高度及解除覆寫的明確介面。
- [x] 3.2 在多圖 controller 建立同列協調器，只將 CSS Grid 同列、相同副圖身分與順序前綴、控制項相容的面板分為同一群組。
- [x] 3.3 採兩階段流程先解除舊覆寫並完成本地量測，再一次套用群組最大值，避免 ResizeObserver 或版面重排迴圈。
- [x] 3.4 在換頁、商品切換、圖表數量切換、模式切換、面板移除及 controller 銷毀時正確註銷群組狀態。

## 4. 完成自動化回歸驗證

- [x] 4.1 驗證各籌碼副圖在完整、部分、無資料及 TDCC 未公布／最近一筆狀態切換時，讀值內容正確且幾何差異不超過 1 CSS px。
- [x] 4.2 驗證序列選取、持股門檻、資料 payload、容器寬度與字型縮放變更會重新計算一次，之後 crosshair 移動維持穩定。
- [x] 4.3 驗證不同副圖順序或控制項設定的面板不會被錯誤納入同一高度群組。
- [x] 4.4 驗證 6 圖與 8 圖仍只能使用單一副圖模式，且單層模式與既有主圖高度不受影響。
- [x] 4.5 執行專案 lint、型別檢查、單元測試、瀏覽器測試與 production build，並修正所有與本變更相關的失敗。

## 5. 完成本機可見行為驗收

- [x] 5.1 在本機以 1、2、3、4 圖多層副圖逐一移動十字線，確認標題列、副圖、下一層副圖及面板 Y 幾何差異皆不超過 1 CSS px。
- [x] 5.2 在 4 圖模式驗證相容面板同列對齊，並驗證不相容面板仍各自穩定、不被錯誤撐高。
- [x] 5.3 驗證響應式縮放、序列切換及大戶／散戶門檻切換只在合法事件重新預留，完成後不再上下跳動。
- [x] 5.4 驗證完整文字可見、無裁切與橫向捲動，十字線 X 偏差不超過 1 CSS px，且沒有新增 request、stream、render generation 或 console error。
- [x] 5.5 驗收後還原測試使用的圖表數量、頁次、商品順序、副圖模式、序列與門檻設定。

## 6. 完成雙站部署驗收與歸檔準備

- [x] 6.1 全部本機 gate 通過後，以同一個已推送 commit 分別部署 Sites 保留站與 Cloudflare 正式站。
- [x] 6.2 在兩個環境重做 1、2、3、4 圖多層副圖的高度穩定與同列對齊驗收，並核對部署版本、靜態資源與 console 狀態。
- [x] 6.3 將測量結果、測試命令、部署版本及兩站驗收證據記錄於 `verification.md`；全部完成後才勾選任務並進行 OpenSpec 歸檔。
