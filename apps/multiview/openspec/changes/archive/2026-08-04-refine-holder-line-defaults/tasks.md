## 1. 預設與偏好遷移

- [x] 1.1 將大戶／散戶 series 預設改為持股比例與週變化，不預設勾選股東人數
- [x] 1.2 提高 defaults 版本並只將上一版完整預設組合遷移為新預設，保留其他客製選擇

## 2. 折線視覺

- [x] 2.1 將大戶／散戶持股比例線與股東人數線設為主圖相同的 1px 線寬
- [x] 2.2 將兩種 holder 折線的資料點半徑統一為主圖 2px，不因單筆資料放大

## 3. 驗證

- [x] 3.1 補齊新預設、舊預設遷移、客製偏好保留與 holder line options 回歸測試
- [x] 3.2 執行相關測試、完整 `npm test`、`npm run lint`、`openspec validate --all --strict` 與 `git diff --check`
- [x] 3.3 在本機瀏覽器驗證大戶／散戶預設不顯示人數線、右鍵仍可開啟，且折線與資料點尺寸正確
