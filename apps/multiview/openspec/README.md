# OpenSpec 工作區

本目錄保存 MultiChartOnCodexSite 的 proposal、design、spec、tasks 與 archive。

- 所有 OpenSpec artifact 使用繁體中文（台灣）。
- 技術名詞、程式碼、API 名稱、路徑、指令與錯誤訊息可保留英文。
- 不得寫入帳號、密碼、API key、token 或金鑰；必要時使用 `[REDACTED_SECRET]`。
- 第一個正式 change 聚焦 `quote-chart-multiview` 的完整 Sites 改寫、功能 parity 清單與部署驗收範圍。
